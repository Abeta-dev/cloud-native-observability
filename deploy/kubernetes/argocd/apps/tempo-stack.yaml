apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tempo-distributed
  namespace: argocd
  labels:
    tier: observability
    component: tracing
spec:
  project: default
  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: tempo-distributed
    targetRevision: 1.9.1
    helm:
      valueFiles:
        - https://raw.githubusercontent.com/umesh0492/cloud-native-observability/v0.1.0/deploy/kubernetes/helm/tempo/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: tracing
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
